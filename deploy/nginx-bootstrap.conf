server {
    listen 80;
    listen [::]:80;
    server_name chovy.online;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 200 'bootstrap: waiting for certificate';
        add_header Content-Type text/plain;
    }
}
